<%= form_with(model: jugador) do |form| %>
  <% if jugador.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(jugador.errors.count, "error") %> prohibited this jugador from being saved:</h2>

      <ul>
        <% jugador.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
