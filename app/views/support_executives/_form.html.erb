<%= form_with(model: support_executive) do |form| %>
  <% if support_executive.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(support_executive.errors.count, "error") %> prohibited this support_executive from being saved:</h2>

      <ul>
        <% support_executive.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
